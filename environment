
DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer

MACOSX_DEPLOYMENT_TARGET=10.9

BOOST_LIBS=filesystem,program_options,system,thread

# Arch is referenced in the shims/sh when it existss
# ARCH="-arch i386 -arch x86_64"
ARCH="-arch x86_64"

CFLAGS="$CFLAGS $ARCH"
CXXFLAGS="$CXXFLAGS $ARCH"
LDFLAGS="$LDFLAGS $ARCH"


# Multiarch
# CFLAGS="$CFLAGS -arch i386 -arch x86_64"
# CXXFLAGS="$CXXFLAGS -arch i386 -arch x86_64"
# LDFLAGS="$LDFLAGS -arch i386 -arch x86_64"
